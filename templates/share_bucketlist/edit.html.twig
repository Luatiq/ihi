{% if not form.vars.data.uuid %}
    <button type="button" class="btn btn-primary" data-action="modal-form#toggleLink">
        {{ 'button.create_link'|trans }}
    </button>
{#    <a href="{{ path('bucketlist.share.toggle_link', { bucketlist: form.vars.data.bucketlist.id }) }}" target="_blank">Create link!</a>#}
{% else %}
    uuid is: {{ form.vars.data.uuid }}
{% endif %}

{{ form_start(form) }}

{{ form_errors(form) }}

{{ form_end(form) }}
