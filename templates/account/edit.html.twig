{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_errors(form) }}

    <button type="submit">Register</button>
    {{ form_end(form) }}
{% endblock %}
